# sop-writer
to execute this project, open terminal in this 'sop-writer' folder and run:
setup python venv-> python -m venv venv
pip install -r requirements.txt
streamlit run app/app.py

to use UI, click on the given link in terminal
to stop running this code, press ctrl+c

for linux/mac, refer to run_in_wsl.sh file